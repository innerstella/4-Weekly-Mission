import Info from "@/src/shared/info";
import Layout from "@/src/shared/layout";
import SearchBar from "@/src/shared/search-bar";

export default function SharedPage() {
  return (
    <Layout>
      <Info />
      <SearchBar />
    </Layout>
  );
}
